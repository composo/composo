Examples
=============

Installation/Usage:
*******************

If `composo` is accessible from a pypi repository install via `pip install composo`, if it is
a git repository you can clone and `pip install -e .` or directly install via `pip install <url-to-repo>.git`.

Example gallery
***************

.. nbgallery::
   notebooks/simple_pipeline
